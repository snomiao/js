/* eslint-disable max-len */
import { L } from "ts-toolbelt";
import { List } from "../List";

// _typeTest();
// function _typeTest() {
//   //
//   snof();
//   snof((n: number) => 2 * n)(444);

//   snof(
//     (n: string) => 2 + n,
//     (n) => Number(n as string) / 2,
//   )(/*  */ "4")
//     .toFixed()
//     .toString() === "12";

//   const fn2 = snof(
//     (n: number) => 2 * n,
//     (n) => String(n * 3),
//   );
//   const a2 = fn2(2).toUpperCase();

//   const fn3 = snof(
//     (n: number) => 2 * n,
//     (n) => n * 3,
//     (n) => n + 1,
//   );
//   const a3 = fn3(2) / 2;
// }
