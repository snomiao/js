// import { Awaitable } from "../types";

// type AwaitableFunction<Args extends ReadonlyArray<any> = [], o = any> = (
//   ...args: Args
// ) => Awaitable<o>;

// // type Curry<fn extends (...args: Args) => R>
// // export async function currya<Args extends any[], R>(fn: (...args: Args) => R): Slice< {
// //   return () => {};
// // }
// type Currya<Fn extends AwaitableFunction> = ()

// // type Params = ReadonlyArray<>;

// function currya<Fn extends AwaitableFunction>(fn: Fn): Currya<Fn> {
//     return
// }

// const fn = currya((a, b) => a * b);
